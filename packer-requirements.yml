---
# Collections only. The role is loaded from the local checkout via
# ANSIBLE_ROLES_PATH=${path.root}/roles set in packer.pkr.hcl, so the AMI
# always reflects the version in the current branch rather than a pinned
# Galaxy release.
collections:
  - ansible.utils
  - community.general
