---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart novnc-desktop
  ansible.builtin.systemd:
    name: novnc-desktop.service
    state: restarted
  listen: Restart novnc-desktop
